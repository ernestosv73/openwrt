name: openwrt

topology:
  nodes:
    openwrt:
      kind: linux
      image: vrnetlab/vr-openwrt:15.05
      
      ports:
        - 8080:80
        - 8443:443
                
      env:
        USERNAME: root #(default)
        PASSWORD: VR-netlab9 #(default is VR-netlab9)
        CONNECTION_MODE: tc #(default)
      # exec:
      # - ip -6 route del default dev eth0
      # - ip addr add 192.168.3.1 dev eth3  
    
    SRV1:
      kind: linux
      image: esanchezv/ubuntu2204:latest
      exec:
       - ip -6 route del default dev eth0
       - ip addr add 192.168.0.10/24 dev eth1
      

  links:
    - endpoints: ["SRV1:eth1", "openwrt:eth1"]    
